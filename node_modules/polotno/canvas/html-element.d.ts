/// <reference types="react" />
import Store, { TextModel } from '../model/store';
declare type ShapeProps = {
    store: typeof Store.Type;
    element: typeof TextModel.Type;
    onClick: Function;
};
export declare const HTMLElement: (({ onClick, element, store }: ShapeProps) => JSX.Element) & {
    displayName: string;
};
export {};

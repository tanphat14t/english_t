/// <reference types="react" />
import { StoreType } from '../model/store';
declare type Props = {
    store: StoreType;
};
export declare const SvgToolbar: (({ store }: Props) => JSX.Element) & {
    displayName: string;
};
export default SvgToolbar;
